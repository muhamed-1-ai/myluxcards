import "server-only"; import { pool } from "../db"; import type { Queryable } from "../db/types"; import type { AppRole } from "@/types/database";
export interface AuditInput {actorId:string|null;actorRole:AppRole;action:string;entityType:string;entityId:string|null;before?:unknown;after?:unknown;ipAddress?:string|null;userAgent?:string|null}
export async function recordAudit(input:AuditInput,db:Queryable=pool){return (await db.query("insert into admin_audit_logs(actor_id,actor_role,action,entity_type,entity_id,before_summary,after_summary,ip_address,user_agent) values($1,$2,$3,$4,$5,$6,$7,$8,$9) returning id,created_at",[input.actorId,input.actorRole,input.action,input.entityType,input.entityId,input.before??null,input.after??null,input.ipAddress??null,input.userAgent??null])).rows[0]}
