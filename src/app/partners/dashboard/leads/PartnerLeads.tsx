"use client";import{FormEvent,useEffect,useState}from"react";
export default function PartnerLeads(){const[data,setData]=useState<any[]>([]),[error,setError]=useState("");async function load(){const r=await fetch("/api/affiliate/leads",{cache:"no-store"});const x=await r.json();if(!r.ok)setError(x.message);else setData(x.data)}useEffect(()=>{load()},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const body:any=Object.fromEntries(f.entries());body.estimatedQuantity=Number(body.estimatedQuantity);body.consentConfirmed=f.get("consentConfirmed")==="on";const r=await fetch("/api/affiliate/leads",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const x=await r.json();if(!r.ok)alert(x.message);else{alert(x.duplicateReview?"Lead registered for duplicate review.":"Lead registered.");e.currentTarget.reset();await load()}}if(error)return <main className="affiliate-section"><div className="affiliate-message affiliate-error">{error}</div></main>;return <main className="affiliate-dashboard"><div className="affiliate-kicker">Business Partner</div><h1>Corporate leads</h1><p>Register only legitimate business opportunities where the contact has agreed to be contacted.</p><form className="affiliate-panel affiliate-fields" onSubmit={submit}><label>Company name<input name="companyName" required/></label><label>Contact person<input name="contactPerson" required/></label><label>Business email<input name="businessEmail" type="email" required/></label><label>Phone<input name="phone" inputMode="tel"/></label><label>Estimated quantity<input name="estimatedQuantity" type="number" min="1" required/></label><label>Interested product ID<input name="productId" placeholder="Optional product UUID"/></label><label>Expected purchase date<input name="expectedPurchaseDate" type="date"/></label><label>Lead source<input name="leadSource"/></label><label className="wide">Notes<textarea name="notes" rows={4}/></label><label className="wide"><span><input name="consentConfirmed" type="checkbox" required/> I confirm a legitimate business purpose and consent to provide these contact details.</span></label><button>Register lead</button></form><section className="affiliate-panel"><h2>Your leads</h2>{data.length?<div className="affiliate-table-wrap"><table className="affiliate-table"><thead><tr>{["Company","Contact","Quantity","Status","Protection","Registered"].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{data.map(x=><tr key={x.id}><td>{x.company_name}</td><td>{x.contact_person}<br/><small>{x.business_email}</small></td><td>{x.estimated_quantity}</td><td>{x.status}</td><td>{x.protection_expires_at?new Date(x.protection_expires_at).toLocaleDateString():"—"}</td><td>{new Date(x.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div>:<p>No leads registered.</p>}</section></main>}
